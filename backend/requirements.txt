# Runtime Python da API/Workers. Ferramentas ofensivas rodam no Kali runner.
# SAST que exige artefato local pode rodar backend-local.
python-slugify==8.0.4
python-dateutil==2.9.0
tabulate==0.9.0
rich==13.7.1
typer==0.12.3
pyyaml==6.0.1
orjson==3.10.3
sh==2.0.7
psutil==5.9.8
setuptools==80.9.0
# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# Configuration & Security
pydantic-settings==2.7.0
pydantic[email]==2.11.0
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.5
bcrypt==4.0.1

# Task Queue
celery==5.4.0
flower==2.0.1
redis==5.2.1
kombu==5.3.7

# LLM & Graph
langgraph==0.2.62
langgraph-checkpoint-postgres==2.0.9
ollama==0.6.1
# SDK Anthropic — opcional; usado apenas quando ANTHROPIC_API_KEY definida.
# O supervisor principal continua em Ollama/qwen. Claude fica disponível para
# tarefas auxiliares (relatórios, análises pontuais) sem mudar o fluxo core.
anthropic==0.54.0

# Vector DB
chromadb==0.5.23

# Embeddings semânticos dos aprendizados HackerOne (ONNX CPU, sem torch)
fastembed==0.4.2

# Visão de saúde dos containers (Centro Operacional → Docker socket, read-only)
docker==7.1.0

# HTTP & Utilities
httpx==0.28.1
requests==2.32.3
urllib3==2.2.3
python-dotenv==1.0.0

# Testing (optional)
pytest==8.3.5
pytest-asyncio==0.23.3

# Backend-local SAST adapter. Used only when source/artifact path is available.
semgrep==1.161.0
